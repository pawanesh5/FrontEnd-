// Create a new paragraph element​ with the "this is new paragraph"

let para = document.createElement("p");
let node = document.createTextNode("This is new paragraph.");
console.log("para:"+node);

// Append the newly created paragraph element to the element with Id content​

 
// Remove the paragraph with id "removeElement​"
    let removepara=document.getElementById('removeElement');
    removepara.remove();
    console.log("para1:"+removepara);
// Change the background-color of the first para element to red

